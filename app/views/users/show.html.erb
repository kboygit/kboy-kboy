<% provide(:title, @user.name) %>
<div class="center jumbotron">
  <!-- This shows the users email,name,created,updated,and time -->
  <!-- <%= @user.name %>, <%= @user.email %>,
  <%= @user.created_at %>, <%= @user.updated_at %>
  <%= Time.now %> -->
  <p>
    USERS
  </p>
</div>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
